services:
  # Ollama LLM service with GPU support
  cyberqueryai-ollama:
    image: ollama/ollama:latest
    container_name: cyberqueryai-ollama
    ports:
      - "11434:11434"
    volumes:
      - cyberqueryai-ollama-data:/root/.ollama
    networks:
      - monitoring
    restart: unless-stopped
    deploy:
      resources:
        reservations:
          devices:
            - driver: nvidia
              count: all
              capabilities: [gpu]
    healthcheck:
      test: ["CMD", "ollama", "list"]
      interval: 30s
      timeout: 10s
      retries: 3
      start_period: 10s

  # CyberQueryAI application service
  cyberqueryai:
    image: cyberqueryai:${ENV:-dev}
    build:
      context: .
      dockerfile: Dockerfile
      args:
        PORT: 443
    container_name: cyberqueryai
    ports:
      - "443:443"
    environment:
      # Load API token hash from .env file (optional - will be generated if missing)
      - API_TOKEN_HASH=${API_TOKEN_HASH:-}
      - OLLAMA_HOST=http://cyberqueryai-ollama:11434
    volumes:
      - ./configuration:/app/configuration
      - ./certs:/app/certs
      - ./logs:/app/logs
    networks:
      - monitoring
    restart: unless-stopped
    depends_on:
      cyberqueryai-ollama:
        condition: service_healthy
    healthcheck:
      test:
        [
          "CMD",
          "sh",
          "-c",
          'python -c "import urllib.request; urllib.request.urlopen(''https://localhost:443/api/health'', context=__import__(''ssl'')._create_unverified_context()).read()"',
        ]
      interval: 30s
      timeout: 10s
      retries: 3
      start_period: 10s

  # Prometheus for metrics collection
  cyberqueryai-prometheus:
    image: prom/prometheus:latest
    container_name: cyberqueryai-prometheus
    ports:
      - "9090:9090"
    volumes:
      - ./prometheus/prometheus.yml:/etc/prometheus/prometheus.yml:ro
      - cyberqueryai-prometheus-data:/prometheus
    command:
      - "--config.file=/etc/prometheus/prometheus.yml"
      - "--storage.tsdb.path=/prometheus"
      - "--web.console.libraries=/usr/share/prometheus/console_libraries"
      - "--web.console.templates=/usr/share/prometheus/consoles"
      - "--web.enable-lifecycle"
    networks:
      - monitoring
    restart: unless-stopped
    depends_on:
      - cyberqueryai

  # Grafana for metrics visualization
  cyberqueryai-grafana:
    image: grafana/grafana:latest
    container_name: cyberqueryai-grafana
    ports:
      - "3000:3000"
    environment:
      - GF_SECURITY_ADMIN_USER=admin
      - GF_SECURITY_ADMIN_PASSWORD=admin
      - GF_INSTALL_PLUGINS=
    volumes:
      - ./grafana/provisioning:/etc/grafana/provisioning:ro
      - ./grafana/dashboards:/var/lib/grafana/dashboards:ro
      - cyberqueryai-grafana-data:/var/lib/grafana
    networks:
      - monitoring
    restart: unless-stopped
    depends_on:
      - cyberqueryai-prometheus

networks:
  monitoring:
    driver: bridge

volumes:
  cyberqueryai-ollama-data:
  cyberqueryai-prometheus-data:
  cyberqueryai-grafana-data:
